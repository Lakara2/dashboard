import React from 'react';
import { Alert } from '@mui/material';

const CriticalCase = () => {
  return (
    <Alert className="error" severity="error">Critical Case</Alert>
  );
};

export default CriticalCase;